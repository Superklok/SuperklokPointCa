version: "2.4"

services:
  app:
    image: superklokpointca:1.10.2
    build: 
      context: .
    ports:
      - "80:80"
    command: node app.js
    volumes:
      - .:/app
    environment:
      - NODE_ENV=development